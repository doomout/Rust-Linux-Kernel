// Rust의 if문
fn main() {
    let condition = true;

    //()가 없는 것이 특징이다.
    if condition == true {
        println!("조건이 참 입니다.");
    } else {
        println!("조건이 거짓 입니다.");
    }
}
/*실행결과
조건이 참 입니다.
 */